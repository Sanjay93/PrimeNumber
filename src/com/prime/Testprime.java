package com.prime;

/**
 * @author Sanjay
 * This is to execute...
 */

public class Testprime {

	public static void main(String args[]) throws NumberFormatException {
		FindPrime n = new FindPrime();
		n.input();
		// ctrl + /
	}
	
}
/**
 *alt shift j  comment
 *clt shift l  shortcuts
 *ctrl d delete line
 *ctrl f find and replace
 *main ctrl space 
 *sysout ctrl space
 *public ctrl space
 *private ctrl space
 *method_name cltrl space
 *window prefernces---editors--- templates
 *project gebrate javadoc
 *source generate tostring, getters and setters, constructors using fields
 *for ctrl space
 *while ctrl space
 *try ctrl space
 *do ctrl space
 *ctrl /  single line comments
 *ctrl shift f formatting to a neat
 * ctrl i intent
 * highlight alt shift z ------lot of templates will come
 * highlight alt shift m  --- will automaticaly create an method ---- most useful 
 * highlight alt arrow keys ----move code up
 * ctrl shift u ----- find ocuurences
 * alt left arrow   === to go to the visitied places previously
 * select the variable and go to refractor and rename ===== everything in package will automatically change
 * window -> preferences-> keys
 * window -> preferences-> editors-> templates-> new -> Define the function 
 * 
 */